package self.day12.aExercise.two;

public interface PersonComparator {
    /**
     * 对两个Person对象进行大小的比较
     * @param person1 参与比较的Person对象
     * @param person2 参与比较的Person对象
     * @return
     *      > 0 : person1 > person2
     *      < 0 : person1 < person2
     *      = 0 : person1 == person2
     */
    /*public abstract */int compare(Person person1, Person person2);
}
